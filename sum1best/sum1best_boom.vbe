-- VHDL data flow description generated from `sum1best_boom`
--		date : Fri Oct  9 19:04:37 2020


-- Entity Declaration

ENTITY sum1best_boom IS
  PORT (
  a : in BIT;	-- a
  b : in BIT;	-- b
  ci : in BIT;	-- ci
  so : out BIT;	-- so
  co : out BIT;	-- co
  vdd : in BIT;	-- vdd
  vss : in BIT	-- vss
  );
END sum1best_boom;


-- Architecture Declaration

ARCHITECTURE behaviour_data_flow OF sum1best_boom IS

BEGIN

co <= ((a AND (ci OR b)) OR (ci AND b));

so <= (a XOR b XOR ci);
END;
